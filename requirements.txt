# Core dependencies for PandID Validation Speckle

# Speckle SDK for working with Speckle objects and API
specklepy>=2.18.0

# String similarity for fuzzy matching in validation rules
python-Levenshtein>=0.23.0

# Data processing for reading and managing rules from spreadsheets
pandas>=1.5.0
numpy>=1.24.0

# Data validation and settings management
pydantic>=2.0.0

# GraphQL client for API queries
gql>=3.4.0
